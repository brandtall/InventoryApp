extends layout

block content
  h1= title
  p Welcome to #[em Our Inventory], a very basic Express website developed as a project for the odin project.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The inventory has the following record counts:

    ul
      li #[strong Items:] !{data.items_count}
      li #[strong Categories:] !{data.category_count}